{
  "name": "@aw-labs/monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --no-cache",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "sass": "^1.53.0",
    "turbo": "^1.3.1",
    "vite": "^2.9.14"
  },
  "engines": {
    "npm": ">=9.1.3",
    "node": ">=18.12.1"
  },
  "packageManager": "npm@9.1.3"
}

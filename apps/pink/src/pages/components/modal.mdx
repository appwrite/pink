---
title: Modals
description: Modals are containers that appear with a scrim (semi-transparent overlay) on top of the window or content the user is currently interacting with. Modals require direct interaction in order to be dismissed.
---

| Class    | Type  |                              |
| -------- | ----- | ---------------------------- |
| `modals` | Modal | A class representing a modal |

## Components

Modals consist of five components:

| Class           | Type    |     |
| --------------- | ------- | --- |
| `modal-form`    | Form    |     |
| `modal-header`  | Header  |     |
| `modal-title`   | Title   |     |
| `modal-content` | Content |     |
| `modal-footer`  | Footer  |     |

<Preview center>
  <only-preview>
    <button
      class="button"
      onclick="document.getElementById('dialog').showModal();"
    >
      <span class="text">Open Modal</span>
    </button>
  </only-preview>
  <only-html>
    <button class="button">
      <span>Open Modal</span>
    </button>
  </only-html>
  <only-preview>
    <dialog
      class="modal"
      id="dialog"
      onclick="document.getElementById('dialog').close();"
    >
      <form
        class="modal-form"
        method="dialog"
        x-on:click="(e) => e.stopPropagation()"
      >
        <header class="modal-header">
          <div class="avatar is-color-orange is-medium">
            <span class="icon-exclamation" aria-hidden="true"></span>
          </div>
          <h4 class="modal-title heading-level-5">Modal title</h4>
          <button class="x-button" aria-label="Close modal">
            <span class="icon-x" aria-hidden="true"></span>
          </button>
        </header>
        <div class="modal-content u-small">Modal label text.</div>
        <div class="modal-footer">
          <div class="u-flex u-main-end u-gap-16">
            <button class="button is-text">
              <span class="text">Button</span>
            </button>
            <button class="button is-secondary">
              <span class="text">Button</span>
            </button>
          </div>
        </div>
      </form>
    </dialog>
  </only-preview>
  <only-html>
    <dialog class="modal" id="dialog">
      <form class="modal-form" method="dialog">
        <header class="modal-header">
          <div class="avatar is-color-orange is-medium">
            <span class="icon-exclamation" aria-hidden="true"></span>
          </div>
          <h4 class="modal-title heading-level-5">Modal title</h4>
          <button class="x-button" aria-label="Close modal">
            <span class="icon-x" aria-hidden="true"></span>
          </button>
        </header>
        <div class="modal-content u-small">Modal label text.</div>
        <div class="modal-footer">
          <div class="u-flex u-main-end u-gap-16">
            <button class="button is-text">
              <span class="text">Button</span>
            </button>
            <button class="button is-secondary">
              <span class="text">Button</span>
            </button>
          </div>
        </div>
      </form>
    </dialog>
  </only-html>
</Preview>

<br />

To open/close the modal, you can find the element by its selector (in this case,
`#dialog`), and call the corresponding method, `showModal()` and `close()`.

e.g.`document.getElementById('dialog').showModal();` and
`document.getElementById('dialog').close();`

Also, make sure to add a click event listener to the modal content, and call
`stopPropagation` on the event, to prevent the modal from closing when the
content is clicked.

## Sizes

In the Appwrite console, we use three sizes of modals:

| class      | Type   |     |
| ---------- | ------ | --- |
| `is-small` | Small  |     |
| -          | Medium |     |
| `is-big`   | Big    |     |

<Preview center>
  <dialog id="dialog4" class="modal is-small" open>
    <form class="modal-form" method="dialog">
      <header class="modal-header">
        <h4 class="modal-title heading-level-5">Modal title</h4>
        <button class="x-button" aria-label="Close modal">
          <span class="icon-x" aria-hidden="true"></span>
        </button>
      </header>
      <div class="modal-content u-small">Modal label text.</div>
      <div class="modal-footer">
        <div class="u-flex u-main-end u-gap-16">
          <button class="button is-text">
            <span class="text">Button</span>
          </button>
          <button class="button is-secondary">
            <span class="text">Button</span>
          </button>
        </div>
      </div>
    </form>
  </dialog>

   <dialog id="dialog5" class="modal is-big" open>
        <form class="modal-form" method="dialog">
            <header class="modal-header">
                <h4 class="modal-title heading-level-5">Modal title</h4>
                <button class="x-button" aria-label="Close modal">
                    <span class="icon-x" aria-hidden="true"></span>
                </button>
            </header>
            <div class="modal-content u-small">Modal label text.</div>
            <div class="modal-footer">
                <div class="u-flex u-main-end u-gap-16">
                    <button class="button is-text">
                        <span class="text">Button</span>
                    </button>
                    <button class="button is-secondary"><span class="text">Button</span></button>
                </div>
            </div>           
        </form>
    </dialog>
</Preview>

## Usage

Modals are used mostly for the creation or deletion of an object. Modals can include avatars, buttons, illustrations, tags and text. For example:

<br />

::IMG[modal-usage-1.svg, modal]

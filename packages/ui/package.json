{
  "name": "@appwrite/ui",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "stylelint src/**/*.scss",
    "format": "stylelint --fix src/**/*.scss",
    "build": "sass --style=compressed --load-path=../../node_modules src/_index.scss:dist/appwrite.css",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@appwrite/icons": "*"
  },
  "devDependencies": {
    "sass": "^1.51.0",
    "stylelint": "^14.8.0",
    "stylelint-config-standard-scss": "^3.0.0"
  }
}
